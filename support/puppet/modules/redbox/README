##redbox using puppet : serverless
__________________________

rpm -ivh http://yum.puppetlabs.com/el/6/products/x86_64/puppetlabs-release-6-7.noarch.rpm
yum install puppet

##After installing puppet, redbox needs to be copied to the module path on the OS.
##Enter: 
sudo -s
puppet config print modulepath 

##to find where to install the redbox module. 
Copy:
- redbox 
- redbox_utilities
- redbox_apache
- redbox_shibboleth (only if using AAF authentication)

##Once redbox is in the module path, to apply it:
puppet module install puppetlabs/concat
puppet module install puppetlabs/stdlib
puppet module install puppetlabs/apache
puppet apply -e "class {'redbox':}"
puppet apply -e "class {'redbox_apache':}" OR puppet apply -e "class {'redbox_shibboleth':}"

License
-------
See file, LICENCE

Contact
-------


Support
-------

Please log tickets and issues at our [Projects site](http://projects.example.com)
