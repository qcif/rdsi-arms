{
  "configured": true,
  "email": "DO_NOT_CHANGE",
  "includeConfigDir": "${fascinator.home}/config-include",
    "includeConfigExt": [
      "json"
    ],
  "redbox.version.string": "${node.theme}-${redbox.version}",
  "urlBase": "${server.url.base}",
  "version": "1.1.0.1",
  "version.string": "${node.theme}-${redbox.version}",
  "version.string": "${redbox.version}",
  "environment": "${server.environment}"
}