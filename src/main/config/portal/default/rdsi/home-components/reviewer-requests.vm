#parseTemplate( "home-components/package-type.vm" )

#if ($self.roleBoard)
    #set($path = "$portalPath/dashboards/reviewer")
#else
    #set($path = "$portalPath/home")    
#end

#if ($self.section)
    #set($section = "${self.section}.vm")
#else 
    #set($section = "active.vm"))    
#end

<div class="nav-tabs-panel">
    <ul class="nav nav-tabs">
        <li class="#if($section=='active.vm') active #end"><a href="$path">Active</a></li>
        <li class="#if($section=='provisioned.vm') active #end"><a href="$path?section=provisioned">Provisioned</a></li>
        <li class="#if($section=='rejected.vm') active #end"><a href="$path?section=rejected">Rejected</a></li>
    </ul>
</div>

#parseTemplate( "home-components/reviewer/$section" )
