<style type="text/css">
#content tr:nth-child(even) td { background: #CCC }
#content td:first-child { width: 15em; border-right: 1px solid; }
.people {
    float: left;
    margin: 0 2em 2em 0;
    width: 45%;
}
#people-container td:first-child { width: 10em; border-right: 1px solid; }
</style>

#macro(displayList $vals)
<ul>
	#foreach( $v in $vals )
	<li>$page.escapeHtml($v)</li> #end
</ul>
#end #set($contentJson = $self.packageData)
<p>
	<span>Citation:</span>$page.escapeHtml($contentJson.getValue("citation"))
</p>

<h4>Description</h4>
<p>$page.escapeHtml($contentJson.getValue("description"))</p>

<h4>Merit description</h4>
<p>$page.escapeHtml($contentJson.getValue("merit-description"))</p>

<div id="people-container">
	<div class="people">
		<h4>Data provider</h4>
		<table>
			<tr>
				<td>Name:</td>
				<td>$page.escapeHtml($contentJson.getValue("dataprovider:foaf:title"))
					$page.escapeHtml($contentJson.getValue("dataprovider:foaf:givenName"))
					$page.escapeHtml($contentJson.getValue("dataprovider:foaf:familyName"))
				</td>
			</tr>
			<tr>
				<td>Role:</td>
				<td>$page.escapeHtml($contentJson.getValue("dataprovider:foaf:role"))</td>
			</tr>
			<tr>
				<td>Organisation:</td>
				<td>$page.escapeHtml($contentJson.getValue("dataprovider:foaf:organization"))</td>
			</tr>
			<tr>
				<td>State:</td>
				<td>$page.escapeHtml($contentJson.getValue("dataprovider:foaf:state:prefLabel"))</td>
			</tr>
			<tr>
				<td>email:</td>
				<td>$page.escapeHtml($contentJson.getValue("dataprovider:foaf:email"))</td>
			</tr>
			<tr>
				<td>phone:</td>
				<td>$page.escapeHtml($contentJson.getValue("dataprovider:foaf:phone"))</td>
			</tr>
		</table>
	</div>
	<div class="people">
		<h4>Data manager</h4>
		<table>
			<tr>
				<td>Name:</td>
				<td>$page.escapeHtml($contentJson.getValue("requester:foaf:title"))
					$page.escapeHtml($contentJson.getValue("requester:foaf:givenName"))
					$page.escapeHtml($contentJson.getValue("requester:foaf:familyName"))
				</td>
			</tr>
			<tr>
				<td>Role:</td>
				<td>$page.escapeHtml($contentJson.getValue("requester:foaf:role"))</td>
			</tr>
			<tr>
				<td>Organisation:</td>
				<td>$page.escapeHtml($contentJson.getValue("requester:foaf:organization"))</td>
			</tr>
			<tr>
				<td>State:</td>
				<td>$page.escapeHtml($contentJson.getValue("requester:foaf:state:prefLabel"))</td>
			</tr>
			<tr>
				<td>email:</td>
				<td>$page.escapeHtml($contentJson.getValue("requester:foaf:email"))</td>
			</tr>
			<tr>
				<td>phone:</td>
				<td>$page.escapeHtml($contentJson.getValue("requester:foaf:phone"))</td>
			</tr>
		</table>
	</div>
</div>
<h4 style="clear: left">Data profile</h4>
<table>
	<tr>
		<td>Primary RDSI node:</td>
		<td>NSW</td>
	</tr>
	<tr>
		<td>Other hosting node:</td>
		<td>$page.escapeHtml($contentJson.getValue("requester:foaf:role"))</td>
	</tr>
	<tr>
		<td>Data format:</td>
		<td>$page.escapeHtml($contentJson.getValue("requester:foaf:organization"))</td>
	</tr>
	<tr>
		<td>Required storage class:</td>
		<td>$page.escapeHtml($contentJson.getValue("requester:foaf:state:prefLabel"))</td>
	</tr>
	<tr>
		<td>Data medium and migration:</td>
		<td>$page.escapeHtml($contentJson.getValue("requester:foaf:email"))</td>
	</tr>
	<tr>
		<td>Required tools and interfaces:</td>
		<td>$page.escapeHtml($contentJson.getValue("requester:foaf:phone"))</td>
	</tr>
	<tr>
		<td>RDSI is the only host?</td>
		<td>
			<ul>
				<li>$contentJson.getValue("storage-risk-rdsi-only")</li>
				<li>$contentJson.getValue("storage-risk-regenerate")</li>
				<li>$contentJson.getValue("storage-risk-disruption")</li>
			</ul>
		</td>
	</tr>
</table>
<h4>Data schedule</h4>
<table>
	<tr>
		<td>ingest 0f 25%:</td>
		<td>$contentJson.getValue("ingest-1qtr")</td>
	</tr>
	<tr>
		<td>ingest 0f 25%:</td>
		<td>$contentJson.getValue("ingest-2qtr")</td>
	</tr>
	<tr>
		<td>ingest 0f 25%:</td>
		<td>$contentJson.getValue("ingest-3qtr")</td>
	</tr>
	<tr>
		<td>ingest 0f 25%:</td>
		<td>$contentJson.getValue("ingest-4qtr")</td>
	</tr>
</table>
<h4>Access and licensing</h4>
<table>
	<tr>
		<td>Access restriction:</td>
		<td>$contentJson.getValue("accessRestrictions")</td>
	</tr>
	<tr>
		<td>Terms of use:</td>
		<td>$contentJson.getValue("dc:rights.skos:note")</td>
	</tr>
	<tr>
		<td>Licence:</td>
		<td>$contentJson.getValue("dc:accessRights")</td>
	</tr>
	<tr>
		<td>Anticipated number of users:</td>
		<td>$contentJson.getValue("user-number")</td>
	</tr>
	<tr>
		<td>Access frequency:</td>
		<td>$contentJson.getValue("user-access-frequency")</td>
	</tr>
</table>
<h4>Other details</h4>
<table>
	<tr>
		<td>Field(s) of research:</td>
		<td>
			#displayList($contentJson.getRepeatables('dc:subject.anzsrc:for','skos:prefLabel'))
		</td>
	</tr>
	<tr>
		<td>Metadata records:</td>
		<td>#displayList($contentJson.getValueList("discover-metadata"))
		</td>
	</tr>
</table>
